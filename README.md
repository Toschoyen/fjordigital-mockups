# fjordigital-mockups

Internal preview host. No public-facing index. Direct mockup URLs are private and shared per-client only.

For internal reference and deploy instructions, see `_internal-index.md` (gitignored).
